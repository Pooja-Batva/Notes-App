import { registerUser, loginUser, logoutUser } from "./user.controller.js";
import { addNote, upadateNote, deleteNote, notes } from "./note.controller.js";

export{
    registerUser,
    loginUser,
    logoutUser,
    addNote,
    upadateNote,
    deleteNote,
    notes,
}